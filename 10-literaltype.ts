/****************************************************
 * LITERAL TYPE IN TS
 * Exactly what value will store it in the variable
 * The value itself is assigned as variable data type.
*/

const str = 'Hey! TypeScript'; // Literal Type
let str2 = 'Hey! JavaScript';
let role: 'admin' | 'read' | 'read-write'; // Literal Type

// role = 'abc';
role = 'admin';