/***************************************
 * TUPLES IN TS
 * TypeScipt tuples is a fixed length, ordered collection of elements where each element has it's specific type.
*/

let employee: [number, string, number, boolean] = [124, 'Shani', 25000, true];

employee.push(100); // exceptional case

console.log(employee);